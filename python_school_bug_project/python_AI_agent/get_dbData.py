def get_dbData(databaseName, tableName, measures):
    """
    :param databaseName:
    :param tableName:
    :param measures:
    :return:  这里的放回的会是元组，第一个参数是执行成功的布尔值1表示成功，第二个参数是对应的表完成修改后的表内容和结构
    """
    data = f"{databaseName},{tableName},{measures}"
    return 'hhhhh',data